# CMake / build
/build/

# Editor / dev (symlink to build/compile_commands.json)
/compile_commands.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Trial logs (kept local, not committed)
/experiments/
/results.csv

# Cursor / Claude Code local config
.cursor/
.claude/
